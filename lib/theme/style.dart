import 'package:flutter/material.dart';

// define app theme
ThemeData appTheme() {
  return ThemeData(
      primaryColor: Colors.orange[900],
      appBarTheme: AppBarTheme(backgroundColor: Colors.orange[900]));
}
