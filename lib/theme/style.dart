import 'package:flutter/material.dart';

// define app theme
ThemeData appTheme() {
  return ThemeData(
      primaryColor: const Color.fromARGB(255, 10, 6, 61),
      appBarTheme:
          const AppBarTheme(backgroundColor: Color.fromARGB(255, 10, 6, 61)));
}
